<resources>
    <!-- Platform theme only bridges to Compose; all real theming lives in ui/theme. -->
    <style name="Theme.EndgameFinance" parent="android:Theme.Material.Light.NoActionBar">
        <item name="android:windowBackground">@color/window_background_light</item>
    </style>
</resources>
