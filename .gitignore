*.iml
.gradle
/local.properties
/.idea/caches
/.idea/libraries
/.idea/modules.xml
/.idea/workspace.xml
/.idea/navEditor.xml
/.idea/assetWizardSettings.xml
.DS_Store
/build
/captures
.externalNativeBuild
.cxx
local.properties

# Qualcomm QNN/NPU native libraries (~20 MB). The NPU path is unused on this
# device (Samsung DSP policy); GPU inference uses the device's own OpenCL.
# Kept on disk locally but out of git history.
/app/src/main/jniLibs/
